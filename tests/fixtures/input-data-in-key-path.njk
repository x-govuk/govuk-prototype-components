{% from "x-govuk/components/decorated/input/macro.njk" import govukInput with context %}

{{ govukInput({
  label: {
    text: "Email address"
  },
  decorate: "data.account.email-address",
  validate: {
    presence: {
      message: "Enter your email address"
    }
  }
}) }}
